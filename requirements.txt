# All runtime dependencies are pulled transitively when the Dockerfile runs
#   pip install ./src/matpy ./src/MANIQA ./src/colorization-metrics
# so this file is intentionally empty. Add lines here only for ad-hoc extras
# (debugging tools, profilers, ...) that should be available inside the IPOL
# image but are not declared in the algorithm's pyproject.toml.
